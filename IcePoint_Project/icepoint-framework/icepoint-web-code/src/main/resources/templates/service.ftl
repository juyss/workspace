package ${code.packageName}.service;

import ${code.packageName}.entity.${code.entityName};
import com.icepoint.framework.web.service.Service;


/**
* ${code.entityName}服务层接口
*
* @author makejava
* @since ${.now}
*/
public interface ${code.entityName}Service extends Service {

        ${code.entityName}Repository get${code.entityName}Repository();


}
